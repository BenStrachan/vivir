<%= simple_form_for([:admin, @assessment_type]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :default_monthly_repeat %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
