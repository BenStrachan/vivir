<%= simple_form_for([:admin, @job_type]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :default_weekly_visits %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
